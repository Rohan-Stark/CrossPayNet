# Tests Directory

This directory is for cross-project test suites, such as end-to-end (E2E) tests or integration tests that span multiple services.

**Rule:** Unit tests and service-specific tests should reside next to their respective code within `apps/` or `services/`.
