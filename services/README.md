# Services Directory

This directory is reserved for backend services with clearly defined responsibilities (e.g., `payment-service`, `ledger-service`).

**Rule:** Do NOT create speculative service directories here. A service should only be added when its architecture has been explicitly planned and approved. 
